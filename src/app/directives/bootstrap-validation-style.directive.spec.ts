import { BootstrapValidationStyleDirective } from './bootstrap-validation-style.directive';

describe('BootstrapValidationStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BootstrapValidationStyleDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
